name: Solid mechanics CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  schedule:
    - cron: '0 8 * * 2'  # run once in a week (here Tue at 8am)

jobs:
  build:

    runs-on: ubuntu-latest
    container:
      image: dealii/dealii:master-noble
      options: --user root
    timeout-minutes: 45

    steps:
    - name: prepare container
      run: sudo apt-get -y update && sudo apt-get -y upgrade && sudo apt-get -y install libboost-all-dev
    - name: build preCICE
      uses: precice/setup-precice-action@main
      with:
        precice-version: develop
        install-prefix: /usr/local
    - uses: actions/checkout@v7
    - name: Adjust user rights
      run: |
        chown -R dealii .
        cd tests/solid
        echo "WORK_DIR=$(pwd)" >> $GITHUB_ENV
    - name: run test

      run: |
        cd $WORK_DIR
        sudo -u dealii cmake -S ../.. -B build -DCMAKE_BUILD_TYPE=Debug
        sudo -u dealii cmake --build build --target solid
        sudo -u dealii ctest --test-dir build -L solid --output-on-failure
      env:
        WORK_DIR: ${{ env.WORK_DIR }}
